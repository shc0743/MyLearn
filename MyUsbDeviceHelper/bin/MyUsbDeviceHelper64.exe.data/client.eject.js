document.getElementById('btn_close').on('click', () => Window.this.close());

// document.$('body').appendChild(document.createTextNode(Window.this.UI_Main.getDevices()))

// const dev = document.getElementById('deviceClasses');
// dev.innerHTML = '';
// const ej = Window.this.UI_Main.getDevices()
// if (ej) for (const i of ej) {
//     const el = document.createElement('option');
//     el.innerText = i.split('|')[1] || i;
//     dev.append(el);
// }